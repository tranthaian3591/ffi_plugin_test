import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Dummy test for ci', () {
    // At least as of now, no true easily-testable features are in
    // flutter_vysma_design. It is mostly just ffi wrappers/convenience.
    // Thus, we leave testing up to vysma_design and the flutter_example.
    expect(true, true);
  });
}
