# Run with `flutter pub run ffigen --config ffigen.yaml`.
name: FlutterVysmaDesignBindings
description: |
  Bindings for `src/flutter_vysma_design.h`.

  Regenerate bindings with `flutter pub run ffigen --config ffigen.yaml`.
output: 'lib/flutter_vysma_design_bindings_generated.dart'
headers:
  entry-points:
    - 'src/flutter_vysma_design.h'
  include-directives:
    - 'src/flutter_vysma_design.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
